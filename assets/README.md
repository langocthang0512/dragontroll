# Game assets

Place source images and JSON data here when external assets are introduced. Register runtime files in `src/game/assets/manifest.ts` so loading remains centralized and observable.

The migrated legacy game still draws its original pixel art procedurally, so this directory intentionally contains no production artwork yet.
