# Player assets

`player-atlas.png` is generated by `npm run assets:build`. Edit the pixel source logic in `tools/generate-player-atlas.mjs`; do not paint over the generated atlas.

The atlas uses 64x64 transparent frames, nearest-neighbor sampling, shared body geometry, and hair-only character variants.
